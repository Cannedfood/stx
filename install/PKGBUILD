# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# The following guidelines are specific to BZR, GIT, HG and SVN packages.
# Other VCS sources are not natively supported by makepkg yet.

# Maintainer: Your Name <youremail@domain.com>
pkgname=stx-git # '-bzr', '-git', '-hg' or '-svn'
pkgver=r131.de84dab
pkgrel=1
pkgdesc="A utility and mostly header-only library"
arch=("x86_64" "x86" "ARM")
url="www.github.com/Cannedfood/stx"
license=('GPL')
makedepends=('git') # 'bzr', 'git', 'mercurial' or 'subversion'
provides=("${pkgname%-git}")
conflicts=("${pkgname%-git}")
source=('git+https://github.com/Cannedfood/stx.git#2.0')
md5sums=('SKIP')

# Please refer to the 'USING VCS SOURCES' section of the PKGBUILD man page for
# a description of each element in the source array.

pkgver() {
	cd "$srcdir/${pkgname%-git}"
	printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

# prepare() {
# 	cd "$srcdir/${pkgname%-git}"
# 	patch -p1 -i "$srcdir/${pkgname%-git}.patch"
# }

# build() {
# 	cd "$srcdir/${pkgname%-git}"
# 	./autogen.sh
# 	./configure --prefix=/usr
# 	make
# }

check() {
	cd "$srcdir/${pkgname%-git}/test/"
	make
}

package() {
	cd "$srcdir/${pkgname%-git}"
	mkdir -p "$pkgdir/usr/include/stx"
	cp stx/*.hpp "$pkgdir/usr/include/stx/"
}
